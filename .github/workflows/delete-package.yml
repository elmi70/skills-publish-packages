name: Delete package

on: [workflow_dispatch]


jobs:
  delete-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/delete-package-versions@v5
        with:
          package-name: 'publish-packages/game'
          package-version-ids: 'sha-863a30d'
          package-type: 'container'
       
